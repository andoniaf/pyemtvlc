[tox]
envlist = flake8,pylintpy36

[testenv]
# install pytest in the virtualenv where commands will be executed
deps =
    pytest
    mock
commands =
    # NOTE: you can run any command line tool here - not just tests
    pytest

[flake8]
max-line-length = 160
